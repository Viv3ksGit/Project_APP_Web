{
  "buildCommand": "cd mobile-app && npm install && npx expo export --platform web",
  "outputDirectory": "mobile-app/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
